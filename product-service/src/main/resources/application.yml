server:
  port: 8081
  servlet:
    context-path: /

spring:
  application:
    name: product-service

logging:
  level:
    com.demo.productservice: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
